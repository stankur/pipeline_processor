# Path to the local SQLite database (relative to worker directory)
NET_DB_PATH=./app.db

# OpenRouter API key for LLM calls (select_highlighted_repos, generate_repo_blurb)
OPENROUTER_API_KEY=sk-or-...

# GitHub token to increase rate limits
GITHUB_TOKEN=ghp_...
